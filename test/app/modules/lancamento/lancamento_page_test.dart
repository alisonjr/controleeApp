import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LancamentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LancamentoPage(title: 'Lancamento')));
    //  final titleFinder = find.text('Lancamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
